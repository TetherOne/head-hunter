version: '3.7'

services:
  app:
    build: .
    container_name: job-hunter
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/job-hunter
    ports:
      - "8001:8000"